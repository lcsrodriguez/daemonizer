[project]
name = "daemonizer"
version = "0.1.0"
description = "Lib to daemonize programs"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "distro>=1.9.0",
]

[dependency-groups]
dev = [
    "pre-commit>=4.6.0",
]
